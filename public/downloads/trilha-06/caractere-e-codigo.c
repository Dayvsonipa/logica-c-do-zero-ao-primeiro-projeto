#include <stdio.h>

int main(void) {
    char letra = 'A';

    printf("Caractere: %c\n", letra);
    printf("Codigo numerico: %d\n", letra);

    letra++;
    printf("Proximo caractere: %c\n", letra);
    return 0;
}
