#include <stdio.h>

int main(void) {
    int numero = 1;

    while (numero <= 5) {
        printf("Repeticao numero %d\n", numero);
        numero++;
    }

    printf("Laco encerrado.\n");
    return 0;
}
